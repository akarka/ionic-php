version: '3.8'

services:
  # PHP ve Apache
  php-server:
    image: php:7.4-apache
    container_name: ionicupload-backend
    ports:
      - "80:80"
    volumes:
      - ./backend:/var/www/html/imageupload
